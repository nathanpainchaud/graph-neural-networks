# @package _global_

defaults:
  - /data/dataset/pyg_base@data.dataset: tu_dataset
  # To add a transform (or pre-transform) to the dataset, you can use the following syntax:
#  - /data/transform@data.dataset: <transform_name>

hparams:
  data:
    # MUTAG dataset contains 135 graphs in the cleaned version, and 188 graphs in the original version
    num_graphs: ${op.ternary:${data.dataset.cleaned}, 135, 188}
    num_node_features: 7
    num_edge_features: 4
    num_classes: 2
