defaults:
  - dataset: ??? # Specify the dataset name

_target_: torch_geometric.data.lightning.LightningDataset
batch_size: 32
num_workers: 0
pin_memory: False
# TODO: Add sampler for train/val/test folds
#       See to support easy switch between (or generation of) multiple splits for cross-validation
